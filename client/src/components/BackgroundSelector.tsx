import React from 'react';

// Simplified background selector that does nothing since we only have white background
export const BackgroundSelector: React.FC = () => {
  return null; // No UI needed since there are no background options
};